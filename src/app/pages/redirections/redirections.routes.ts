import { Routes } from '@angular/router';
import { RedirectionsComponent } from './redirections.component';

export const REDIRECTIONS_ROUTES: Routes = [
  { path: '', component: RedirectionsComponent },
];
